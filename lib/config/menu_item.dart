enum TopMenuItem {
  changeLanguage,
  themeModeLight,
  themeModeDark,
  keepScreenOnDisable,
  keepScreenOnEnable,
  settings,
  help,
}

enum TrackMenuItem {
  changeKeyboardKey,
  changeName,
  delete,
}

enum RowMenuItem {
  playbackMode,
  playbackSpeed,
  playbackVolume,
  playbackBalance,
  delete,
}

enum AllTracksMenuItem {
  playbackModeSet,
  playbackSpeedSet,
  playbackVolumeSet,
  playbackBalanceSet,
  titleReset,
  shortcutKeyReset,
  recordingsDelete,
}
