enum TrackMenuItem {
  changeKeyboardKey,
  trackRecordingMove,
  changeName,
  delete,
}

enum RowMenuItem {
  playbackMode,
  playbackSpeed,
  playbackVolume,
  playbackBalance,
  playbackStartAtPositionReset,
  playbackEndAtPositionReset,
  recordingsDelete,
}

enum AllTracksMenuItem {
  playbackModeSet,
  playbackSpeedSet,
  playbackVolumeSet,
  playbackBalanceSet,
  playbackStartAtPositionReset,
  playbackEndAtPositionReset,
  titleReset,
  shortcutKeyReset,
  // settingsReset,
  // recordingsDelete,
}
