enum TrackMenuItem {
  keyboardKeyChange,
  recordingMove,
  nameChange,
  recordingDelete,
}

enum RowMenuItem {
  playbackModeSet,
  playbackSpeedSet,
  playbackVolumeSet,
  playbackBalanceSet,
  playbackStartAtPositionReset,
  playbackEndAtPositionReset,
  recordingsDelete,
}

enum AllTracksMenuItem {
  playbackModeSet,
  playbackSpeedSet,
  playbackVolumeSet,
  playbackBalanceSet,
  playbackStartAtPositionReset,
  playbackEndAtPositionReset,
  moreSettings,
}
